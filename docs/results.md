# Results

Raw per-run output is written under `results/<run-id>/` (gitignored) and collected on the
local machine — the pod has no git credentials, so nothing is pushed from it.

A committable reporting format (tables comparing models and hardware) will be defined from
this output once we have real runs.
