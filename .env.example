# Local secrets for the RunPod orchestrator. Copy to .env (gitignored) and fill in.
# .env is loaded by scripts/orchestrate_pod.py; real env vars take precedence.

RUNPOD_API_KEY=
# Path to the private SSH key whose public key is registered with your RunPod account.
SSH_KEY_PATH=
