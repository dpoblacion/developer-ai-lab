You are continuing a Spec Driven Development project.

Read `docs/acceptance-criteria.md`. Write **automated tests** that verify the acceptance
criteria: backend tests for the API and its persistence behavior, and at least basic
frontend tests.

Backend tests must run with `dotnet test` from the repository root. Run them and fix
failures until they pass.
