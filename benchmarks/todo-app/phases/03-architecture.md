You are continuing a Spec Driven Development project.

Read `docs/spec.md` and `docs/acceptance-criteria.md`. Design the architecture using
this stack: .NET 10 with ASP.NET Core Minimal APIs, React + TypeScript, .NET Aspire for
orchestration, PostgreSQL for storage, Redis for caching, and OpenTelemetry for
observability.

Define the solution and project layout explicitly. The backend solution must build with
`dotnet build` from the repository root; the frontend lives in `frontend/`. Explain how
the pieces communicate and how observability is wired.

Save the design to `docs/architecture.md`.
