You are continuing a Spec Driven Development project.

Read the documents in `docs/`. Add a **.NET Aspire AppHost** that orchestrates the
services: the API, the React frontend, PostgreSQL, and Redis, wiring configuration and
service discovery between them.

The solution must still build with `dotnet build` from the repository root. Run it and
fix errors until it succeeds.
