You are continuing a Spec Driven Development project.

Do a **final review** of the application against `docs/acceptance-criteria.md`. Check the
backend builds (`dotnet build`), tests pass (`dotnet test`), and the frontend builds
(`npm run build` in `frontend/`).

Write `docs/review.md` listing which acceptance criteria are met, any gaps, and the
overall state. Fix any critical issues you find.
