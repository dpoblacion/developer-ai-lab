You are continuing a Spec Driven Development project.

Read the documents in `docs/`. Implement the **backend** as designed: an ASP.NET Core
Minimal API (.NET 10) for todos, persisting to PostgreSQL via EF Core, using Redis for
caching, and instrumented with OpenTelemetry.

Follow `docs/plan.md`. The backend solution must build with `dotnet build` from the
repository root. Run `dotnet build` and fix errors until it succeeds.
