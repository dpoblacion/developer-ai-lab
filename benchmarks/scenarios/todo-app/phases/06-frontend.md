You are continuing a Spec Driven Development project.

Read the documents in `docs/` and the backend you implemented. Build the **frontend** in
`frontend/`: a React + TypeScript app that consumes the todos API and lets users create,
list, complete, and delete todos.

The frontend must build with `npm run build` from inside `frontend/`. Run the build and
fix errors until it succeeds.
