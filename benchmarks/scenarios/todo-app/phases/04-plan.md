You are continuing a Spec Driven Development project.

Read `docs/spec.md`, `docs/acceptance-criteria.md`, and `docs/architecture.md`. Write an
**implementation plan**: an ordered list of small, independently verifiable tasks that
together deliver the app described in the architecture (backend, frontend, Aspire
orchestration, tests).

Save it to `docs/plan.md`.
