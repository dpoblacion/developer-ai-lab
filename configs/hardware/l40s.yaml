# Single NVIDIA L40S (48GB). Cheap validation GPU. A100 80GB is the capacity fallback.
gpu_type_ids:
  - "NVIDIA L40S"
  - "NVIDIA A100 80GB PCIe"
gpu_count: 1
price_usd_per_hour: 0.79      # RunPod community on-demand (edit when prices change)
gpu_memory_utilization: 0.92
